#include<stdio.h>
main()
{
  printf("taille de char est %d\n",sizeof(char));
  printf("taille de int est %d\n",sizeof(int));
  printf("taille de short int est %d\n",sizeof(short int));
  printf("taille de long int est %d\n",sizeof(long int));
  printf("la taille de float est %d\n",sizeof(float));
  printf("la taille de double est %d\n",sizeof(double));
  printf("la taille de long double %d\n",sizeof(long double));
}
